// licensed with CC BY-NC-SA 4.0 https://creativecommons.org/licenses/by-nc-sa/4.0/
// by Débora Falleiros Gonzales
// https://www.gonzalesdebora.com/

a.show()

s0.initCam(0)
s1.initCam(1)
s2.initCam(2)

src(s0).out(o0)
src(s1).out(o1)
src(s2).out(o2)

src(s2).out(o2)
src(s1).colorama(() => Math.PI * mouse.x /.7).out(o2)
//src(s1).kaleid(() => Math.PI * mouse.y /180).out(o1)
src(s1).modulate(noise(3),()=>a.fft[2]).out(o0)
src(s0).out(o1)


speed = 0.127;
shape(20, 0.2, 0.3)
	.color(0.5, 0.8, 83.13)
	.scale(() => Math.sin(time) + 1 * 2)
	.repeat(() => Math.sin(time) * 10)
	.modulateRotate(o0)
	.scale(() => Math.sin(time) + 1 * 1.5)
	.modulate(noise(2, 2))
	.rotate(1, .2)
	.out(o0);

src(o1).blend(o1).blend(o2).out(o3)


render()
